<Window x:Class="CaliburnExample.Views.Login.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CaliburnExample.Views.Login"
        mc:Ignorable="d"
        Title="Login" Width="200" SizeToContent="Height">
    <Border Padding="15">
        <StackPanel>
            <StackPanel.Resources>
                <Style TargetType="{x:Type TextBox}">
                    <Setter Property="Padding" Value="5" />
                </Style>
            </StackPanel.Resources>

            <TextBlock Text="Username" />
            <TextBox Text="{Binding Username, UpdateSourceTrigger=LostFocus}" />

            <TextBlock Text="Password" />
            <TextBox Text="{Binding Password, UpdateSourceTrigger=LostFocus}" />

            <TextBlock Text="{Binding ResultMessage, UpdateSourceTrigger=PropertyChanged}" Foreground="#e74c3c" />
            <Button x:Name="Login" Margin="0 10 0 0" Padding="5" Content="Login" />

        </StackPanel>
    </Border>
</Window>
